
//Here are some guiding questions:

//1 What event listeners do you need to start off with?
    // - add bookmark
    // - remove bookmark

//2 Can you correctly add bookmarks?
//3 What UI element allows a user to remove a previously added bookmark?
//4 How can you make sure that the correct bookmark is removed?

const body = document.body;
const bUrl = document.querySelector(".url");
const bName = document.querySelector(".bname");
const bList = document.querySelector(".b-list");
const bAdd = document.querySelector(".btn-add");
const bRemove = document.querySelector('.btn-remove')




const = bookmarks [];

const removeBookmark = document.getElementById();
favoritesMarkDel.addEventListener('click', )
// set variable for bookmarks list in element in html
// add event listener for clicking delete of bookmark , push new bookmarks array with changes
// show/render updated favorites list

const addBookmarkButton = document.getElementById();
addBookmarkButton.addEventListener('click', ()=> {
    // get bookmark name

    //get bookmark address

    //push object values {name, address}

    // show/render updated favorites list
});

const renderMarks = () => {
    const bookmarkList = document.getElementById();
    bookmarks.map()

    bookmarkList.appendChild();
}
// set variable for bookmarks list element in html
// map bookmarks array with new html list item for bookmark name and address
// pass to html with innerhtml
// append to bookmarklist
